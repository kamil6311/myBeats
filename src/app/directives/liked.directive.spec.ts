import { LikedDirective } from './liked.directive';

describe('LikedDirective', () => {
  it('should create an instance', () => {
    const directive = new LikedDirective();
    expect(directive).toBeTruthy();
  });
});
